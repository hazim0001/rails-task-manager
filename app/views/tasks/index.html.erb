<h2>All tasks</h2>


<h4> <%= link_to 'Add new task', tasks_new_path %> </h4>
<ul>

  <% @tasks.each do |task| %>
  <li> <%= link_to task.title, task_path(task) %>  - <%= task.details%> - <%= task.completed %> </li>
  <% end %>

</ul>
